---
name: xiaohongshu-publish
description: 小红书笔记从近30天同类内容趋势研究、内容预审到发布执行的完整流程：按主题检索公开可见样本，筛选可核验的点赞 Top20，分析标题、正文及封面/轮播图/公开视频关键帧的结构与风险，提炼当前时点内容概要，再生成标题、正文、话题和配图建议，并选择手动、半自动或浏览器自动化发布路径。使用场景：用户要求从主题生产小红书内容、研究同类爆文、做去 AI 味与发布前审、发布或批量整理笔记、检查标题/话题/封面缺失、交接封面或图文视觉、打开 creator.xiaohongshu.com 填写笔记、设置定时发布，或排查发布流程问题。
---

# 小红书笔记发布

本技能按下面的标准流程执行。**只要本次任务包含趋势研究、同类内容、爆文、Top20、当前热门等调研要求，或用户只给主题/需求并要求产出内容，站内检索就是硬性前置门禁：第一可观察动作必须使用内置浏览器打开小红书站内搜索页、公开笔记页或用户提供的小红书链接。不得先调用通用互联网搜索、不得先写文案、不得用互联网搜索结果替代站内样本。**需要浏览器交互时，按浏览器控制技能选择并使用浏览器；只有实际打开站内页面并记录访问结果后，才能进入样本采集。站内访问失败必须停止并取得用户对公开网页降级的明确授权。仅当用户提供完整内容且没有要求趋势研究时，才可跳过站内检索。核心原则：**先确认事实和内容承诺，再补齐发布字段；缺什么补什么，修复后才生成发布包**。平台硬限制、样本观察、经验性建议和待核验事实必须分开标注。

## 安装与环境准备（默认流程）

- 安装或首次使用本技能时，**默认运行** `scripts/setup.ps1`：检查 Python 3 / Node.js / Edge，并把
  `playwright-core` 安装到仓库本地 `node_modules`（需要联网），最后做加载验证。
- 仅当用户明确要求跳过或环境离线时才跳过，并在回复中说明未运行原因；检查出现 FAIL/WARN 时，先按提示补齐再进入发布流程，无法补齐的如实报告缺口。

## 标准流程

1. **收集输入**：判断输入类型，解析五要素和必要的事实/视觉字段。
2. **趋势研究（硬性站内门禁）**：只要任务包含趋势/同类/爆文/Top20/当前热门调研，或用户只给主题并要求产出，第一动作必须使用浏览器实际打开小红书站内搜索页、公开笔记页或用户提供的小红书链接，并先记录 `site_search_status: started` 及访问结果；未完成这一步不得写文案、提炼结论或调用通用互联网搜索。随后按 [references/trend-research.md](references/trend-research.md) 检索近30天同类公开样本，按可见点赞数形成 Top20，并同时分析可实际读取的标题/正文、封面、轮播图和公开视频关键帧。站内访问失败时，必须停止并先取得公开网页调研的明确授权，不能静默降级。
3. **提炼概要**：从样本中区分事实、观察和假设，提炼当前时点的目标读者、痛点、内容角度、标题触发器、结构和互动动作；不得复制样本原文、封面或专属模板。
4. **生成初稿**：按 [references/content-production.md](references/content-production.md) 生成标题、正文、话题、配图建议和视觉交接字段；没有真实材料时保留 `待补材料/待核验`。
5. **内容预审**：按 [references/content-preflight.md](references/content-preflight.md) 检查目标、核心机制、真人材料、标题承诺和两层风险；输出 `可发 / 需修改 / 暂不建议发`。
6. **解析门禁**：补齐缺失项并让用户确认解析结果；未通过门禁不生成发布包。
7. **生成发布包**：运行 `scripts/build_publish_pack.py` 生成「操作清单」+「直接复制版」。脚本负责确定性字段检查和候选扫描，不替代语义复核。
8. **选择发布方式与发布时间（必选）**：内容生成后、进入发布环节前，**必须先询问用户**选择发布方式（手动 / 半自动 / 自动）和发布时间（立即发布 / 指定时间/定时发布）；未选择不得进入填写环节，选定后按 [references/publish-paths.md](references/publish-paths.md) 执行。
9. **平台填写与最终确认**：按 [references/platform-notes.md](references/platform-notes.md) 填写；提交或点发布前必须把账号、标题、正文、话题、封面、时间和预审状态列给用户确认。
10. **执行发布与核验**：仅在用户明确回复“确认发布/自动发布”后提交；自动化路径点击发布或定时发布后，检查成功提示、跳转页面或作品列表，记录最终状态和可见链接。未完成核验时不得声称已发布。

## 收集输入（第 1 步明细）

### 1.1 判断输入类型

- **完整内容**：对话中直接给出标题/正文/话题/封面路径 → 直接解析。
- **素材文件**：给出素材文件路径（如 `<工作区>\小红书发布素材\文字\01_工位免费3个月\01_工位免费3个月.txt`）→ 读取并解析。
  素材按「类型 + 主题」存放：`小红书发布素材\<图片|视频|文字>\<主题>\`；每个主题在三个子文件夹下各建一个同名文件夹。
- **仅主题/需求**（如“帮我写一篇关于跨境电商补贴的科普”）→ 先按 [references/trend-research.md](references/trend-research.md) 研究近30天同类样本，再按 [references/content-production.md](references/content-production.md) 提炼概要和产出内容。
- **仅主题/需求** → 先确认或合理假设目标读者、要解决的问题和本篇增长任务（拉新 / 建立信任 / 强化标签）；没有真实经历、数据或来源时使用占位符并标记 `待补材料`，不得编造。
- **内容分散**：内容分布在不同消息或旧文件 → 先汇总合并，再解析。
- **批量任务**：同时记录输入素材、加工中的选题/初稿/视觉 brief、可发布库存和已排输出；库存不足时降低承诺，不把固定日更当成完成标准。

### 1.2 解析五要素

| 要素 | 校验规则 |
|---|---|
| 标题 | 必填；检查是否 ≤20 字，超限记下待与用户确认（或换用备选标题） |
| 备选标题 | 可选；缺失不阻塞 |
| 正文 | 保留换行与 emoji，不自行改写 |
| 话题标签 | 通常 5-7 个；缺失或过少时提示补充，或按正文内容建议 |
| 封面图 | 至少 1 张；缺失时标记，进入补齐步骤 |

内容字段还要补充：目标读者/问题、标题触发器、单一核心机制、证据与授权、封面承诺、主要互动动作。它们缺失时可先标为 `待确认`，但高风险主张、精确数字、案例和商业关系不能默认为已确认。

### 1.3 补齐缺失项

- 缺封面 → 向用户要路径，或按配图规范生成/从配图库选用；
- 缺发布方式或定时时间 → 询问用户选择：手动 / 半自动 / 自动，以及「立即发布」还是指定时间；
- 缺话题 → 提示用户补充或按正文内容建议；
- 缺备选标题 → 可留空，不阻塞流程。
- 缺真实依据 → 保留为 `待核验`，不把数字、收益、效果、案例、政策或身份写成事实。
- 需要图片或封面 → 先确认精确标题、图片来源/授权、画面中的事实和不可新增的数字，再交给视觉流程；没有实际生成或保存时如实记录运行状态。

### 1.4 校验确认（门禁）

- 五要素和必要事实字段就绪后，把提取结果整理给用户确认一遍；内容不完整先补齐再走，补完才进入生成发布包。
- 预审结果与发布门禁不可互相替代：预审确认内容是否可进入发布准备；最终确认确认登录账号、发布时间和页面实际填写结果。

## 目录约定

生成/整理的内容统一放在 `<工作区>\小红书发布素材\` 下，按类型与主题组织：

```
<工作区>\小红书发布素材\
├── 图片\<主题>\      # 封面、轮播图、实拍图、配图库
├── 视频\<主题>\      # 视频素材/成品
└── 文字\<主题>\      # 素材 txt、初稿、发布操作清单、直接复制版
```

主题文件夹沿用 `NN_主题` 命名（如 `01_工位免费3个月`）；某主题没有对应类型时只建实际用到的子文件夹，不创建空目录。

## 输入约定

素材文件推荐格式（脚本可直接解析，节标题可带后缀，如 `【发布参数（第一篇 · 试发布）】`）：

【标题】
【备选标题】
【正文】
【话题标签】
【配图建议】
【发布参数】（含 `封面图：路径` 等）

没有素材文件时，直接在对话中给出标题/正文/话题标签/封面路径即可，助手用直传参数生成发布包。

## 关键规则

- 标题不超过 20 字；超限先与用户确认，或换用备选标题。
- 5-7 个话题、6-8 页图文、每周几篇等是起始建议，不是平台硬规则；按账号阶段和数据复盘调整。
- 发布前必须向用户确认完整内容、预审状态、登录账号与定时时间。默认先停在填写完成页面；半自动模式下发布按钮留给用户点击，浏览器自动化模式只有在用户明确确认发布后才代为点击，并在点击后核验结果。
- 生成内容后必须先让用户选择发布方式和发布时间（手动 / 半自动 / 自动；立即发布 / 指定时间），未确认前不得进入发布填写与提交环节。
- 关键词命中只是候选扫描；必须结合意图、证据、授权、商业关系和标题—封面—正文一致性做语义复核。禁止用谐音、拼音、拆字、表情或图片藏字规避审核。
- **趋势调研站内门禁**：每次趋势研究必须先尝试打开小红书站内搜索页、公开笔记页或用户提供的站内链接，并记录实际访问结果；不得在未尝试站内访问时直接使用通用互联网或搜索引擎调研。只有站内访问失败（包括用户未在登录等待期内完成登录）且用户明确确认“使用公开网页调研”时，才可降级；降级结果必须写明研究路径、失败原因和局限，且不得称为小红书站内 Top20。
- “点赞 Top20”只表示在指定时间窗、指定检索范围和可见数据下筛出的公开样本，不得表述为小红书全站官方排名；无法看到发布时间、点赞数或原文时不得补猜。
- **视觉样本分析**：趋势调研不限于标题和正文。对已实际打开且公开可见的笔记，可结合封面、轮播图、截图和视频关键帧识别画面主体、图内可读信息、信息顺序、证据类型和视觉节奏；只用可看清的内容，无法辨认时标 `待核验`。只概括可迁移结构，不逐字转录图中文字、不复制图片或视频帧，也不从模糊画面推断未显示的事实。
- 趋势研究提炼结构和用户问题，不复制标题、正文、封面文字、图片、账号身份或专属案例；样本中的数据、政策、收益和案例不能直接转写为新文案事实。
- 浏览器优先级与登录处理：研究和发布均严格按“内置浏览器 → 外置 Edge/Chrome → Playwright + Edge 桥接”执行。必须先实际尝试内置浏览器并记录 `browser_route: in_app`；只有内置浏览器发生明确技术失败（工具不可用、导航调用报错、页面无法加载等）才可进入下一层，不能仅因外置浏览器已有登录态或桥接已启动而跳级。出现“登录后查看”或扫码页不算技术失败：停留在内置浏览器请求用户登录，默认每 10-30 秒检查一次、最多等待 120 秒；等待期间不得切换浏览器。
- 修复优先补披露/证据、收窄断言、最小改写；仍无法支持时删除或暂缓，不承诺绝对安全或一定不被限流。
- 当前会话没有内置浏览器控制工具时，先记录 `browser_route: in_app_unavailable` 及不可用原因；然后尝试外置 Edge/Chrome。只有内置和外置浏览器都明确不可用时，才可按 [references/publish-paths.md](references/publish-paths.md) 方案三使用 Playwright + Edge 调试端口桥接。不得把“存在桥接脚本”或“桥接已启动”当作跳过内置浏览器的理由，也不要擅自修改全局配置。
- 官方定时发布通常更容易人工复核，但不等于零风险；不要把周一/三/五等节奏建议写成平台规则。
- 营销类表述（如“免费”“扣1”）可能触发平台营销声明，发布时需用户手动处理。

## 脚本

- `scripts/setup.ps1`：安装/首次使用时默认运行——一键检查环境（Python 3 / Node.js / Edge）并把
  `playwright-core` 安装到仓库本地 `node_modules`（需要联网）。
- `scripts/build_publish_pack.py`：生成发布操作清单与直接复制版，执行必填/字数/文件检查，并输出风险候选警告 + 待确认清单。候选警告不等于平台判定。示例：
  - 素材文件：`python build_publish_pack.py --source "<工作区>\小红书发布素材\文字\01_工位免费3个月\01_工位免费3个月.txt" --time "明天 10:10"`（默认输出到该素材所在主题文件夹）
  - 直接传参：`python build_publish_pack.py --title "标题" --body "正文" --tags "#话题1 #话题2" --cover "封面.jpg" --theme "主题名" --num "02" --time "立即发布"`
- `scripts/pw-bridge.cjs`：Playwright + Edge 调试端口桥接服务（js_repl 关闭时自动化发布用），用法见
  [references/publish-paths.md](references/publish-paths.md) 方案三；`playwright-core` 优先使用仓库本地
  `node_modules`，其次自动探测 Codex 运行库，也可用环境变量 `PW_PLAYWRIGHT_MODULES` 指定。
