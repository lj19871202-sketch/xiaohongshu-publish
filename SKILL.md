---
name: xiaohongshu-publish
description: 小红书笔记发布参考流程（内容由用户提供，不固定）：收集并校验标题/正文/话题/封面或素材文件，按标准流程完成发布包生成（操作清单+直接复制版）、发布方式选择（手动/半自动/浏览器自动化）、创作者平台表单填写与发布前确认。使用场景：用户要求发布小红书笔记、整理多篇素材并校验（标题字数、话题数量、封面缺失）、生成小红书发布素材与配图、打开 creator.xiaohongshu.com 检查登录并填写发布笔记、设置定时发布，或遇到小红书发布相关的登录、表单填写、浏览器自动化问题。
---

# 小红书笔记发布

本技能是**参考流程**：内容不固定，每次由用户输入（标题、正文、话题标签、配图路径，或整篇素材文件），助手按下面的标准流程执行。核心原则：**缺什么补什么，补完才生成发布包**。

## 标准流程

1. **收集输入**：判断输入类型 → 解析五要素 → 补齐缺失 → 校验确认，明细见下节。
2. **生成发布包**：运行 `scripts/build_publish_pack.py` 生成「操作清单」+「直接复制版」（含 ChatGPT 半自动指令）。支持两种输入：素材文件（`--source`）或直接传参（`--title/--body/--tags/--cover`）。脚本会输出标题字数、封面文件、话题数量等校验警告，并打印「发布前待确认清单」。
3. **选择发布方式**：手动 / 半自动（推荐）/ 浏览器自动化，按 [references/publish-paths.md](references/publish-paths.md) 判断。
4. **平台填写与确认**：按 [references/platform-notes.md](references/platform-notes.md) 填写；提交或点发布前必须把标题、正文、话题、封面、时间列给用户确认。

## 收集输入（第 1 步明细）

### 1.1 判断输入类型

- **完整内容**：对话中直接给出标题/正文/话题/封面路径 → 直接解析。
- **素材文件**：给出素材文件路径（如 `<工作区>\小红书发布素材\01_工位免费3个月\01_工位免费3个月.txt`）→ 读取并解析。
  素材按主题存放：`小红书发布素材\NN_主题\` 文件夹内放该主题的 txt 与配图。
- **仅主题/需求**（如“帮我写一篇关于跨境电商补贴的科普”）→ 先按 [references/content-production.md](references/content-production.md) 的调研/文案流程产出内容，再回到解析。
- **内容分散**：内容分布在不同消息或旧文件 → 先汇总合并，再解析。

### 1.2 解析五要素

| 要素 | 校验规则 |
|---|---|
| 标题 | 必填；检查是否 ≤20 字，超限记下待与用户确认（或换用备选标题） |
| 备选标题 | 可选；缺失不阻塞 |
| 正文 | 保留换行与 emoji，不自行改写 |
| 话题标签 | 通常 5-7 个；缺失或过少时提示补充，或按正文内容建议 |
| 封面图 | 至少 1 张；缺失时标记，进入补齐步骤 |

### 1.3 补齐缺失项

- 缺封面 → 向用户要路径，或按配图规范生成/从配图库选用；
- 缺定时时间 → 询问「立即发布」还是指定时间；
- 缺话题 → 提示用户补充或按正文内容建议；
- 缺备选标题 → 可留空，不阻塞流程。

### 1.4 校验确认（门禁）

- 五要素就绪后，把提取结果整理给用户确认一遍；内容不完整先补齐再走，补完才进入第 2 步。
- 注意与第 4 步「发布前最终确认」的区别：本步确认的是**解析结果**（内容是否正确提取、有无缺失）；第 4 步确认的是**最终发布状态**（登录账号、发布时间、表单填写结果）。两步不互相替代。

## 输入约定

素材文件推荐格式（脚本可直接解析，节标题可带后缀，如 `【发布参数（第一篇 · 试发布）】`）：

【标题】
【备选标题】
【正文】
【话题标签】
【配图建议】
【发布参数】（含 `封面图：路径` 等）

没有素材文件时，直接在对话中给出标题/正文/话题标签/封面路径即可，助手用直传参数生成发布包。

## 关键规则

- 标题不超过 20 字；超限先与用户确认，或换用备选标题。
- 发布前必须向用户确认完整内容与定时时间；半自动模式下发布按钮留给用户点击。
- 当前会话没有浏览器控制工具时，**不要擅自修改全局配置**（如 `js_repl`/Computer Use 开关，曾观察到与所用模型冲突）；
  优先按 [references/publish-paths.md](references/publish-paths.md) 方案三用「Playwright + Edge 调试端口」桥接实现自动化
  （不依赖 js_repl/Computer Use，本技能 `scripts/pw-bridge.cjs` 提供桥接服务）；确实无法接入时再退回半自动或手动路径。
- 官方「定时发布」最稳定、零风控；发布节奏建议每周一/三/五。
- 营销类表述（如“免费”“扣1”）可能触发平台营销声明，发布时需用户手动处理。

## 脚本

- `scripts/build_publish_pack.py`：生成发布操作清单与直接复制版，并输出校验警告 + 待确认清单。示例：
  - 素材文件：`python build_publish_pack.py --source "<工作区>\小红书发布素材\01_工位免费3个月\01_工位免费3个月.txt" --time "明天 10:10"`
  - 直接传参：`python build_publish_pack.py --title "标题" --body "正文" --tags "#话题1 #话题2" --cover "封面.jpg" --theme "主题名" --num "02" --time "立即发布"`
- `scripts/pw-bridge.cjs`：Playwright + Edge 调试端口桥接服务（js_repl 关闭时自动化发布用），用法见
  [references/publish-paths.md](references/publish-paths.md) 方案三。
